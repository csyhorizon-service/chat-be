# CSY Horizon - 채팅 서비스 아키텍처 및 개발 계획서

본 문서는 `csyhorizon/chat-be` 마이크로서비스의 설계 및 개발 계획을 정의합니다. 보안 통신, 휘발성 데이터 저장, WebRTC 확장성 및 안전한 신고 메커니즘에 중점을 둡니다.

## 1. 아키텍처 개요 및 기술 스택
- **프레임워크:** Spring Boot + Spring WebFlux (비동기 논블로킹, 실시간 채팅에 적합)
- **데이터 저장소 (채팅 내역):** Redis (In-Memory 저장소, 1일 TTL 적용)
- **영구 저장소 (신고 내역):** RDBMS (MySQL/PostgreSQL 등 - 기존 DB 활용)
- **인증 및 인가:** 기존 `auth-be` 마이크로서비스 연동 (토큰 기반 인증)
- **통신 프로토콜:** WebSocket / RSocket (WebFlux 지원)
- **클라이언트 지원:** Web (현재) -> Mobile App (확장 예정)

## 2. 핵심 요구사항 및 해결 방안

### A. 채팅 데이터 보관 및 파기 (Redis)
- **휘발성 저장:** 채팅 메시지는 RDBMS에 저장하지 않고 Redis의 List 또는 Sorted Set을 사용하여 메모리에만 저장합니다.
- **자동 만료 (TTL):** 각 채팅방 또는 메시지에 24시간(1일)의 TTL(Time-To-Live)을 설정하여, 1일이 경과하면 Redis 시스템에 의해 자동으로 삭제되도록 구현합니다.

### B. 양방향 암호화 통신 (종단간 암호화, E2EE)
서버가 메시지 내용을 볼 수 없거나 중간에 가로챌 수 없도록 **종단간 암호화(End-to-End Encryption)** 를 기본으로 적용합니다.
1. **키 교환:** 클라이언트(웹/앱)는 최초 접속 시 공개키/개인키 쌍을 생성합니다.
2. **서버의 역할:** 서버는 클라이언트들의 '공개키'만 보관하고 중계합니다.
3. **메시지 전송:** 발신자는 수신자의 공개키(또는 교환된 대칭키)로 메시지를 암호화하여 서버로 보냅니다. 서버는 이를 단순 중계만 하며, 수신자만이 자신의 개인키로 이를 복호화할 수 있습니다. 서버 데이터베이스(Redis)에 저장되는 내용도 모두 암호화된 상태입니다.

### C. 메시지 신고 및 데이터 위변조 방지 
서버가 메시지를 해독할 수 없으므로, 유일한 예외인 '신고(Report)' 발생 시 데이터 변조를 막기 위한 메커니즘이 필수적입니다.
- **메시지 전자 서명 (Digital Signature):** 
  - 발신자가 메시지를 보낼 때, 자신의 '개인키'로 암호화된 메시지(또는 원본 평문 해시)에 대한 **전자 서명**을 생성하여 함께 전송합니다.
- **신고 프로세스:**
  1. 수신자(신고자)는 신고하려는 메시지의 **암호문, 원본 평문, 해당 메시지의 복호화 키, 그리고 발신자의 전자 서명**을 서버의 '신고 API'로 전송합니다.
  2. 서버는 저장해둔 발신자의 '공개키'를 이용해 전달받은 **전자 서명을 검증**합니다. 
  3. 서명 검증이 통과되면, 해당 메시지가 실제 발신자가 보낸 것이며 **신고자에 의해 고의로 위변조되지 않았음이 증명**됩니다.
  4. 검증이 완료된 데이터만 영구 DB(RDBMS)에 저장하여 관리자가 확인할 수 있도록 처리합니다.

### D. WebRTC 기반 음성/화상 통화
WebRTC 통합 시 서버 부하 분산이 가장 중요합니다. 메신저와 같은 서버에서 처리할지 여부에 대한 기준입니다.
- **시그널링 (Signaling - 접속 정보 교환):** WebRTC 연결을 맺기 위한 정보(SDP, ICE Candidate) 교환은 단순한 텍스트 데이터 통신입니다. 이는 **현재 구축할 WebFlux 채팅 서버에서 처리해도 부하가 거의 없습니다.** 따라서 채팅 서버 내에 통합해도 무방합니다.
- **미디어 중계 (TURN/SFU - 실제 음성/영상 트래픽):** 실제 음성과 영상 데이터가 서버를 거쳐가는 중계 방식은 트래픽과 CPU 부하가 기하급수적으로 높습니다.
- **결론 및 계획:** 
  - **시그널링 기능은 현재 WebFlux 서버에 포함**하여 작업합니다.
  - **미디어 통신 자체는 P2P(Peer-to-Peer)** 로 직접 연결하게 하여 서버를 거치지 않도록 구성합니다. 만약 다대다 통화나 방화벽 문제로 중계 서버가 반드시 필요한 경우에는, 채팅 서버에 기능을 넣지 않고 **완전히 별도의 미디어 전용 서버(예: Coturn)를 분리 구축**하는 것을 원칙으로 합니다. (요청하신 대로 부하가 심한 부분은 채팅 서버 작업에서 제외합니다.)

## 3. 개발 단계 (Milestones)

- **Phase 1: 프로젝트 셋업 및 인증 연동**
  - Spring WebFlux + Redis 환경 구성 (`chat-be`)
  - `auth-be`와의 JWT 인증 연동 및 웹소켓 연결 보안 설정
- **Phase 2: E2EE 채팅 코어 및 휘발성 저장 구현**
  - 공개키 교환 API 및 웹소켓 메시지 송수신 채널 구현
  - Redis를 활용한 메시지 저장 및 1일(24시간) TTL 만료 처리 로직 구현
- **Phase 3: 신고 시스템 및 암호학적 위변조 검증**
  - 클라이언트-서버 간 메시지 서명 첨부 프로토콜 정의
  - 신고 API 구현 (서명 검증, 평문 대조 로직 적용)
  - 검증 완료된 신고 데이터 DB 저장 처리
- **Phase 4: WebRTC 시그널링 지원**
  - WebRTC Peer Connection 생성을 위한 시그널링 전용 웹소켓 엔드포인트 추가 (미디어 서버 트래픽 완전 배제)
